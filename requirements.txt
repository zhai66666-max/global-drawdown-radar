# ── 合并自三个旧仓库的依赖并集 ───────────────────────────────────────────────
# nasdaq-etf-monitor + global-drawdown-radar + nasdaq100-daily-report
# 版本下限按 Python 3.11+ / 3.13 实测可跑为准，不锁死小版本以免 Actions 上装不上。

requests>=2.31.0          # 所有 HTTP 抓取（腾讯行情 / 东财 / DeepSeek / FRED）
PyYAML>=6.0               # config/*.yaml
python-dotenv>=1.0.0      # 本地 .env
pandas>=2.2.0             # 行情序列与回撤计算
numpy>=1.26.0             # 计算底座
yfinance>=0.2.40          # 全球 ETF 长历史（radar 用；Actions 上可用）
duckdb>=1.0.0             # 长期行情库 data/market.duckdb
Jinja2>=3.1.3             # templates/brief.html

# 说明：
#  1) 三个旧仓库各自锁了不同的 pandas/yfinance 版本，这里取并集下限。
#     pandas 3.x 有 breaking change（Timestamp 相减行为变化），
#     代码里已按 2.x/3.x 都能跑的方式写，未锁 <3。
#  2) 不需要 matplotlib：原 radar 的画图部分未纳入邮件（邮件用纯 HTML 表格渲染）。
#  3) 不需要 schedule/APScheduler：定时交给 GitHub Actions cron。
